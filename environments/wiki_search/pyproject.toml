[project]
name = "wiki-search"
version = "0.1.0"
dependencies = [
    "verifiers>=0.1.2",
    "chromadb",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["wiki_search.py"]
