[project]
name = "vf-tool-test"
version = "0.1.1"
requires-python = ">=3.11,<3.13"
dependencies = [
    "verifiers>=0.1.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["vf_tool_test.py"]
