[project]
name = "vf-smolagents-math-tools"
version = "0.1.0"
dependencies = [
    "verifiers>=0.1.2",
    "smolagents>=1.15.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["vf_smolagents_math_tools.py"]
