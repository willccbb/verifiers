[project]
name = "reasoning-gym-env"
version = "0.1.0"
dependencies = [
    "verifiers>=0.1.2",
    "reasoning-gym",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["reasoning_gym_env.py"]
