[project]
name = "vf-reasoning-gym"
version = "0.1.0"
dependencies = [
    "verifiers>=0.1.2",
    "reasoning-gym",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["vf_reasoning_gym.py"]
